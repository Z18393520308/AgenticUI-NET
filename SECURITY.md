# Security Policy

## 当前安全边界

AgenticUI.NET 的 WPF、WinForms 和 `AgenticUI.Remote` 默认只提供本机 Named Pipe 传输，
不监听 TCP。远程服务默认要求随机令牌认证；`.NET 8` 还会把管道限制在当前操作系统用户。
敏感控件值默认不写入日志或操作录制。

可选的独立 `AgenticUI.Gateway` 只接受 WSS/TLS，并使用与本机 Pipe 不同的令牌。Gateway
默认只允许读取和引导类动作，写操作需显式加入白名单；UDP 发现默认关闭且不接收认证或控制
命令。部署者仍须配置可信证书、防火墙、秘密存储、业务授权和高风险操作二次确认。完整清单见
[Gateway 安全部署指南](docs/gateway.zh-CN.md)。

应用内鼠标动作不会移动系统指针，只向本进程中已注册、当前可展示的 WPF/WinForms 控件发送
鼠标消息。坐标超出控件、目标被遮挡、控件禁用或不在当前模态交互窗口时会拒绝执行。由于鼠标
动作仍可能改变业务数据，Gateway 默认白名单不包含这些动作，宿主应用应按控件和业务场景授权。

连接令牌相当于本次应用会话的控制权限。不要把它写入源码、普通日志、命令行历史或版本
控制。生产应用应通过仅授权用户可见的本机渠道传递令牌，并为高风险业务动作增加自身的
权限检查和二次确认。

## 报告问题

请不要在公开 Issue 中披露仍可利用的安全漏洞。优先使用 GitHub 仓库的私密漏洞报告
功能；如果该功能尚未启用，请先提交不包含漏洞细节的 Issue，请求维护者建立私密联系。

当前 `0.4.0` 尚未完成第三方安全审计，不应把 AgenticUI.NET 本身视为业务授权系统。

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| 0.4.x | 支持 |
| 0.3.x | 仅接受严重安全修复 |
| 0.2.x | 仅接受严重安全修复 |
| 0.1.x | 不再主动支持 |

安全更新优先发布到最新的 `0.4.x`。生产项目应固定经过验证的补丁版本，并关注仓库的
Security Advisories 和 Release Notes。
