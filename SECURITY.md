# Security Policy

## 当前安全边界

AgenticUI.NET 当前只提供本机 Named Pipe 传输，不监听 TCP。远程服务默认要求随机令牌
认证；`.NET 8` 还会把管道限制在当前操作系统用户。敏感控件值默认不写入日志或操作
录制。

连接令牌相当于本次应用会话的控制权限。不要把它写入源码、普通日志、命令行历史或版本
控制。生产应用应通过仅授权用户可见的本机渠道传递令牌，并为高风险业务动作增加自身的
权限检查和二次确认。

## 报告问题

请不要在公开 Issue 中披露仍可利用的安全漏洞。优先使用 GitHub 仓库的私密漏洞报告
功能；如果该功能尚未启用，请先提交不包含漏洞细节的 Issue，请求维护者建立私密联系。

当前 `0.3.0` 尚未完成第三方安全审计，不应把 AgenticUI.NET 本身视为业务授权系统。

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| 0.3.x | 支持 |
| 0.2.x | 仅接受严重安全修复 |
| 0.1.x | 不再主动支持 |

安全更新优先发布到最新的 `0.3.x`。生产项目应固定经过验证的补丁版本，并关注仓库的
Security Advisories 和 Release Notes。
